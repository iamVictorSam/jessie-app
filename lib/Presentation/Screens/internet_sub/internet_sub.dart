import 'package:flutter/material.dart';
import 'package:jessiepay/Presentation/Screens/internet_sub/components/body.dart';

class InternetSub extends StatelessWidget {
  const InternetSub({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: InternetSubBody(),
    );
  }
}
