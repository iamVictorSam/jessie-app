import 'package:flutter/material.dart';

class P2PWithdrawal extends StatelessWidget {
  const P2PWithdrawal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
